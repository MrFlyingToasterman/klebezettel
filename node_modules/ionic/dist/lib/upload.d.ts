import { DeploySnapshotRequest, IonicEnvironment } from '@ionic/cli-utils';
export declare function upload(env: IonicEnvironment, {note, channelTag}: {
    note?: string;
    channelTag?: string;
}): Promise<DeploySnapshotRequest>;
