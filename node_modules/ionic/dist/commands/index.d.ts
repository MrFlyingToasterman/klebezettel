import { CommandMap, IonicEnvironment, Namespace, NamespaceMap } from '@ionic/cli-utils';
export declare class IonicNamespace extends Namespace {
    root: boolean;
    name: string;
    source: string;
    namespaces: NamespaceMap;
    commands: CommandMap;
    runCommand(env: IonicEnvironment): Promise<void>;
}
